# Tarefa: IDs obrigatórios em UUID

- **Status:** done
- **Data:** 2026-07-11
- **Slug:** `20260711-uuid-ids`

## Objetivo

Tornar UUID a estratégia obrigatória de primary key em toda a aplicação. Proibir IDs sequenciais (`serial` / autoincrement). Documentar a regra e alinhar schema auth + Better Auth + helpers.

## Fora de escopo

- Migrar dados legados existentes (se IDs atuais não forem UUID: reset/re-seed do ambiente local).
- Criar tabelas de domínio novas além do schema auth.
- Mudanças de UI.

## Apps / packages tocados

- [ ] `apps/server`
- [ ] `apps/web`
- [x] `packages/db`
- [x] `packages/auth`
- [ ] `packages/contracts`
- [ ] `packages/types`
- [x] `packages/utils`
- [x] outro: `AGENTS.md`, `tasks/TEMPLATE.md`, `.cursor/rules/*`, `.agents/skills/create-task`

## Skills obrigatórias (ler antes de implementar)

- [x] `create-task` (este fluxo)
- [ ] `chakra-ui-builder` (se houver UI)
- [x] `better-auth-best-practices` (se houver auth)
- [ ] `turborepo` (se houver pipeline/cache)

## Checklist backend (se aplicável)

Fluxo: Route → Controller → Service → Repository → Drizzle.

- [x] Schema em `packages/db` (IDs auth → `uuid`)
- [x] Colunas reutilizáveis de ID + soft-delete em `packages/db`
- [x] Helper `createId` em `packages/utils`
- [x] Better Auth: `advanced.database.generateId: "uuid"`
- [ ] Contracts/types em packages (só DTOs/tipos — sem regra de negócio)
- [ ] `*Repository.ts` / `*Service.ts` / `*Controller.ts` (N/A — só infra)
- [ ] `*.routes.ts` (N/A)

## Checklist frontend (se aplicável)

- [ ] N/A

## Justificativa de desvio (só se necessário)

Nenhum desvio de arquitetura. Auth continua em `packages/auth` (config); schema em `packages/db` (sem regra de negócio).

## Critérios de aceite

- [x] Docs (`AGENTS.md`, TEMPLATE, rules, create-task) exigem UUID e proíbem serial/sequencial.
- [x] Schema auth usa `uuid` em PKs e FKs.
- [x] Better Auth gera IDs com `generateId: "uuid"`.
- [x] Helper `createId()` disponível em `@sync_v2/utils`.
- [x] Typecheck `packages/db`, `packages/auth`, `packages/utils` OK (`npm run check` falha por artefatos `apps/web/build` pré-existentes).

## Progresso

- [x] Criar tarefa
- [x] Atualizar documentação
- [x] Schema + utils + auth
- [x] Validar typecheck packages
