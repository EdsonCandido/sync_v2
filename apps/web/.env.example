# Local (npm run dev): API no host :3000 — ver docs/LOCAL.md
# Docker full-stack / produção: use a URL pública da API (ex. http://localhost:13000 ou HTTPS) — ver docs/PRODUCAO.md
VITE_SERVER_URL=http://localhost:3000
